# Auth

El acceso administrativo actual usa una cookie `HttpOnly` firmada y se valida en
`/admin`. Las credenciales `Admin` / `Admin` son exclusivamente de desarrollo y
deben sustituirse por usuarios persistentes y contraseñas cifradas antes de
publicar.
